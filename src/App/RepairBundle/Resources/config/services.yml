parameters:
    app.repair.count_unread_message: App\RepairBundle\Service\CountUnReadMessage
services:
    app.repair.count_unread_message:
        class: %app.repair.count_unread_message%
        arguments:
            - @service_container
            - @

    message_repository:
        class: App\RepairBundle\Entity\RepairMessageRepository
        arguments:
            - RepairBundle:RepairMessage
#    app.repair.example:
#        class: %app.repair.example.class%
#        arguments: [@service_id, "plain_value", %parameter%]
#        calls:
#            - [setService, [@service_id]]
#        tags:
#            - { name: twig.extension }
#            - { name: kernel.event_listener, event: kernel.exception, method: onKernelException }