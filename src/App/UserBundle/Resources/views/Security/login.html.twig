{% extends "RepairBundle::layout.html.twig" %}

{% block title %}登录{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style type="text/css">
        body{

            min-height: 600px;
            background-image: url("{{ asset('bundles/repair/img/Background-Gradient.jpg') }}");
            background-repeat:no-repeat;
            background-size:100% 100%;
            background-origin:content-box;
        }


        .navbar-brand{
            float: left;
        }
        .bottom {
            clear: both;
            height: 40%;
            bottom: 0;
            position: absolute;
            width: 100%;
            text-align: center;
            z-index: -99;
            overflow:hidden
        }
        .bottom img{
            margin-left: 10%;
            margin-right: 10%;
            height: 100%;
            max-width: 1500px;

        }
        #_submit{
            width: 80px;
            background-color:#227ABF ;
            color: #ffffff;
        }

        .checkbox{
            min-width: 50px;
        }

        div p{
            font-size: 60px;
            color: #ffffff;

            padding-bottom:30px;



            text-align: center;
        }

        #welcome{
            padding-top: 60px;
            color: #ffffff;
            clear: both;

            text-align: center;
        }
        #login{


        }
        label{
            color: #ffffff;
        }
        #errorMsg
        {

            color:#ffff00;
        }
        .checkbox label{
            color:#ffffff;
        }
    </style>
{% endblock %}

{% block nav %}

    <a class="navbar-brand" href="{{ path('homepage') }}"><img src="{{ asset('bundles/repair/images/Logo-white.png') }}" alt="logo" width="180" /></a>

{% endblock %}

{% block body %}


    <div id="welcome" class="col-sm-6 col-sm-offset-3">
        <h4>欢&nbsp;&nbsp;迎&nbsp;&nbsp;使&nbsp;&nbsp;用</h4>
    </div>

    <div class="col-sm-6 col-sm-offset-3">
        <p>办公报修系统</p>
    </div>

    {% trans_default_domain 'FOSUserBundle' %}


<div id="login" class="col-sm-4 col-sm-offset-4">
    <form class="form-horizontal " action="{{ path("fos_user_security_check") }}" method="post">
            <div class="col-sm-10 col-sm-offset-2 form-group">
                {% if error %}
                    <div id="errorMsg" class="col-sm-12"><h4>{{ error.messageKey|trans(error.messageData, 'security') }}</h4></div>
                {% endif %}
                <div class="form-group">
                    <label for="username" class="col-sm-4 control-label">用户名</label>
                    <div class="col-sm-8">
                        <input type="text" class="form-control col-sm-12" id="username" name="_username" placeholder="UserName:" value="{{ last_username }}" required="required">
                    </div>
                </div>
            </div>

            <div class="col-sm-10 col-sm-offset-2 form-group">
                <div class="form-group">
                <label for="password" class="col-sm-4 control-label">密码</label>
                <div class="col-sm-8">
                    <input type="password" class="form-control col-sm-12" id="password"  name="_password" required="required" placeholder="Password:">
                </div>
                </div>
            </div>
            <div class="form-group col-sm-12">
                <div class="col-sm-4 col-sm-offset-3">
                    <div class="checkbox">
                        <label>
                            <input type="checkbox" id="remember_me" name="_remember_me" value="on">{{ 'security.login.remember_me'|trans }}
                        </label>
                    </div>
                </div>
                <div class="col-sm-4">
                    <input type="submit" id="_submit" name="_submit" class="btn" value="{{ 'security.login.submit'|trans }}" />
                </div>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        </form>



</div>
{% endblock %}

{% block footer %}

<div class="bottom">
    <img src="{{ asset('bundles/repair/img/indexBottom.png') }}" alt="items" />
</div>

{% endblock %}


{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function() {


        })
    </script>

{% endblock %}
